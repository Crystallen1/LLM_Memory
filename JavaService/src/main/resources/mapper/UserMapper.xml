<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="org.crystallen.lc.mapper.UserMapper">
    <insert id="insertUser" useGeneratedKeys="true" keyProperty="id" parameterType="org.crystallen.lc.entity.Users">
        INSERT INTO Users
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="username!=null">
                username,
            </if>
            <if test="email!=null">
                email,
            </if>
            <if test="password!=null">
                password,
            </if>
            <if test="createdAt!=null">
                created_at,
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="username != null" >
                #{username ,jdbcType=VARCHAR},
            </if>
            <if test="email != null" >
                #{email ,jdbcType=VARCHAR},
            </if>
            <if test="password != null" >
                #{password ,jdbcType=VARCHAR},
            </if>
            <if test="createdAt != null" >
                #{createdAt ,jdbcType=TIMESTAMP},
            </if>
        </trim>
    </insert>
    <select id="findByUsernameOrEmail" parameterType="String" resultType="org.crystallen.lc.entity.Users">
        SELECT * FROM Users WHERE username = #{usernameOrEmail} OR email = #{usernameOrEmail}
    </select>
    <select id="findByUsername" parameterType="String" resultType="org.crystallen.lc.entity.Users">
        SELECT * FROM Users WHERE username = #{username}
    </select>

    <select id="findByEmail" parameterType="String" resultType="org.crystallen.lc.entity.Users">
        SELECT * FROM Users WHERE email = #{email}
    </select>
</mapper>